{
  "name": "api-gateway",
  "version": "2.0.0",
  "private": true,
  "description": "OpenAI-compatible aggregate API gateway",
  "type": "module",
  "engines": {
    "node": ">=22"
  },
  "scripts": {
    "start": "node index.mjs config.json"
  }
}
